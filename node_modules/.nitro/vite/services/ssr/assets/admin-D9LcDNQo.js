import { n as getServerEnv } from "../index.js";
import { t as getServerSupabase } from "./server-8eKJVEFT.js";
import { createFileRoute, lazyRouteComponent } from "@tanstack/react-router";
import { createServerClient } from "@supabase/ssr";
//#region src/lib/supabase/auth.ts
function parseCookies(header) {
	if (!header) return [];
	return header.split(";").map((pair) => {
		const idx = pair.indexOf("=");
		return {
			name: pair.slice(0, idx).trim(),
			value: pair.slice(idx + 1).trim()
		};
	});
}
/**
* Build a Supabase client bound to a server Request's cookies so we can read
* the current admin session during a route loader. Read-only: we never set
* cookies inside a loader.
*/
function getServerSupabaseFromRequest(request) {
	const env = getServerEnv();
	return createServerClient(env.supabaseUrl, env.supabaseAnonKey, {
		cookies: {
			getAll: () => parseCookies(request.headers.get("cookie")),
			setAll: () => {}
		},
		auth: {
			autoRefreshToken: false,
			persistSession: false
		}
	});
}
/**
* Returns the authenticated admin user, or null if not logged in / invalid.
* Used by the /admin loader to gate the dashboard.
*/
async function getAdminUser(request) {
	try {
		const { data: { user } } = await getServerSupabaseFromRequest(request).auth.getUser();
		return user;
	} catch {
		return null;
	}
}
//#endregion
//#region src/routes/admin.tsx
var $$splitComponentImporter = () => import("./admin-DrHl3hbN.js");
var Route = createFileRoute("/admin")({
	loader: async ({ request }) => {
		const user = await getAdminUser(request);
		if (!user) return {
			user: null,
			stats: null
		};
		const supabase = getServerSupabase();
		const [{ count: enquiries }, { count: workshops }, { data: recent }] = await Promise.all([
			supabase.from("consultations").select("*", {
				count: "exact",
				head: true
			}).in("status", ["new", "contact"]),
			supabase.from("workshop_registrations").select("*", {
				count: "exact",
				head: true
			}),
			supabase.from("consultations").select("name,current_role_name,target_role_name,status,created_at").order("created_at", { ascending: false }).limit(6)
		]);
		return {
			user: { email: user.email },
			stats: {
				enquiries: enquiries ?? 0,
				workshops: workshops ?? 0,
				recent: recent ?? []
			}
		};
	},
	component: lazyRouteComponent($$splitComponentImporter, "component")
});
//#endregion
export { Route as t };
